const{test,expect}=require('@playwright/test');
test('controlled pilot child journey makes no Supabase calls',async({page})=>{await page.addInitScript(()=>{window.__cloudCalls=[];window.supabase={createClient:()=>({auth:{getSession:async()=>{window.__cloudCalls.push('auth');return{data:{session:null}}}},rpc:async(name)=>{window.__cloudCalls.push(name);return{data:null,error:null}}})}});await page.goto('/learn.html');await expect(page.getByRole('heading',{name:'Choose a mission. Leave a trace.'})).toBeVisible();expect(await page.evaluate(()=>window.__cloudCalls)).toEqual([]);await page.goto('/lesson.html?m=what-is-ai');await expect(page.locator('#agePill')).toContainText('10');expect(await page.evaluate(()=>window.__cloudCalls)).toEqual([])});
test('Arabic Journey has its own RTL URL and remains usable on mobile',async({page})=>{await page.setViewportSize({width:390,height:844});await page.goto('/ar/learn.html');await expect(page.locator('html')).toHaveAttribute('dir','rtl');await expect(page.getByRole('heading',{name:'اختر مهمة. واترك أثرًا.'})).toBeVisible();await expect(page.getByRole('link',{name:'ابدأ مهمتك الأولى'})).toBeVisible();await expect(page.getByText('الفئة التجريبية: من ١٠ إلى ١٢ سنة.')).toBeVisible()});